defmodule CloudControlServer.ServiceHandlerTest do
  use ExUnit.Case

  test "starts a new node with a given config" do
    raise "test not written"
  end

  test "updates the config" do
    raise "test not written"
  end

  test "triggers a callback when the service node is ready" do
    raise "test not written"
  end

  test "get the current status of the node" do
    raise "test not written"
  end
end
